import { NETWORK_RPC_URL } from 'constants/envs';

const RPC = {
  4: `${NETWORK_RPC_URL}`,
  1: `${NETWORK_RPC_URL}`,
  5: `${NETWORK_RPC_URL}`,
  97: 'https://data-seed-prebsc-2-s3.binance.org:8545',
};

export default RPC;
